import { describe } from "vitest";

describe("Solution box component", () => {
  describe("drag and drop", () => {
    it("drags block when pointer down", () => {
      // const block = <Block name={""} />;
    });
    it("puts block in exact slot", () => {
      // const parentId = newUUID();
      // const parent = <Block id={parentId} name={"testing-dnd"} />;
      // const child = <Block name={"test-child"} />;
    });
  });
});
